# custom-content

SMB_Config -> Check the config in smb.conf and package will set the correct values

Settings afected : "workgroup","security","client min protocol","client max protocol","encrypt passwords","smb encrypt","smb signing"

Sensor -> SMB Configuration
Package -> Set SMB V3 Configuration